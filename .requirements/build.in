pelican
markdown
minchin.pelican.plugins.autoloader >= 1.2.3  # to not break on PEP440 versioning
minchin.pelican.plugins.cname
minchin.pelican.plugins.nojekyll
minchin.pelican.plugins.optimize_images
minchin.pelican.plugins.post_stats
minchin.pelican.plugins.summary >= 1.2.0
pelican-alias
pelican-extended-sitemap
# pelican-minify  # breaks jquery
pelican-neighbors
seafoam >= 2.19.0  # for fat footer, main page article listing control
# seafoam @ git+https://github.com/MinchinWeb/seafoam.git@master
typogrify
webassets >= 0.12.1
html5lib

# for beautifulsoup
lxml

# explicity required on GitHub CI Linux builds
pytz

# security forced updated
jinja2 >= 3.1.6  # otherwise, via Pelican
Pillow >= 10.2.0
Pygments >= 2.7.4  # otherwise, via Pelican
lxml >= 4.6.5

invoke
minchin.text
pip-tools
